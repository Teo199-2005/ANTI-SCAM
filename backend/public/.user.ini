; Per-directory PHP limits (Apache/CGI when user_ini is enabled).
; php artisan serve ignores this — use `composer run dev` or `composer run serve` so PHP gets -d flags from composer.json.
upload_max_filesize = 128M
post_max_size = 260M
